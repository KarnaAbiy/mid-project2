var score = 0;
function clicker(){
    score += 1;
    document.getElementById("score").innerHTML = score;

    switch(score) {
        return Math.floor(Math.random() * 2);
    }         
}
